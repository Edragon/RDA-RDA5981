1、如果要进行写efuse和TX/RX收发测试，请把测试模式下的feature文件夹复制到SDK根目录进行替换，正常模式下不允许写efuse和TX/RX收发测试。
1、如果只是对校准相关的寄存器进行操作，把正常模式下的feature文件夹复制到SDK根目录进行替换。正常模式下可以配置寄存器，读efuse。如果配置寄存器时顺便把配置的值保存到了flash中，系统会在启动后第一次进行联网操作时将相应的值从flash中读取出来，并配置到对应的寄存器。SDK默认是正常模式。